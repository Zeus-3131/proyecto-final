export const calculateTotal = (req, res) => {
    // Implementar la lógica para calcular el total a pagar de todos los productos en el carrito del usuario
    res.status(200).send({ message: "Total calculado" });
  };
  